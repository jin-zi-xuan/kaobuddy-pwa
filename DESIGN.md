# KaoBuddy 视觉系统

## Design Read

面向长期考研学生的高频学习工作台，语气冷静但有人味，采用克制的产品化界面，不使用营销页式炫技。

## 三个参数

- `DESIGN_VARIANCE: 6`：桌面端有明确不对称主次，移动端回归单列。
- `MOTION_INTENSITY: 4`：只保留进入、切换和操作反馈。
- `VISUAL_DENSITY: 6`：能快速扫读计划和资料，但不做后台式信息堆积。

## 色彩

- 画布：柔和中性灰白 `#f3f5f1`
- 主表面：`#fbfcf9`
- 主文字：`#172521`
- 品牌墨绿：`#244c40`
- 唯一强调色：珊瑚橙 `#c85c42`
- 深色模式使用同一色相体系，不做分区主题翻转

强调色只用于当前重点、关键反馈、错误和高优先级。普通成功状态使用品牌墨绿，不额外引入第二种鲜艳颜色。

## 字体与数字

- 使用系统中文无衬线字体，避免外部字体阻塞和中文回退跳动。
- 标题依靠字重、行距和负字距建立层级，不使用超大营销标题。
- 倒计时、进度和分钟数使用 tabular nums。
- 正文控制在 65-75 个中文字符的舒适行长内。

## 形状

- 控件 8px 圆角
- 内容面板 12-16px 圆角
- 只有圆形进度与数量标记使用全圆角
- 不使用大面积玻璃拟态、霓虹光和悬浮卡片墙

## 布局

- 首次进入采用分屏：左侧是价值和真实学习场景，右侧完成建档。
- 工作台采用左侧主导航和右侧内容区。
- 今天页优先展示倒计时、总进度、今日工作量和唯一当前任务。
- 计划页用三状态学习块组织任务。
- 视频页把 16:9 播放器与随手记放在同一个学习上下文中。
- 780px 以下侧栏折叠为顶部横向导航，多列内容全部回到单列。

## 动效

- 页面进入使用 8px 位移 + 透明度，约 320ms。
- 按钮按下使用 `scale(0.98)`。
- 只动画 `transform` 与 `opacity`。
- `prefers-reduced-motion: reduce` 下关闭非必要动画。

## 可访问性

- 主要触控目标至少 44px。
- 表单标签始终显示，不用 placeholder 替代标签。
- 焦点环使用单一强调色，保持清晰可见。
- 支持系统明暗模式。
- B 站 iframe 提供标题，并保留官方播放器的键盘能力。
