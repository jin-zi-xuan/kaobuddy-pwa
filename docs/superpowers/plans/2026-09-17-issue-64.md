# #64 可靠性修复与验证

目标：修复运行检查发现的具体问题，保持现有学习流程，不做大范围重构。

用户已批准上一轮检查提出的方向，并要求完成修复、自行验证。本轮直接执行。

- [x] 数据安全：备份入口放在初始化页和侧栏；默认按 ID 合并且保留本地同 ID 记录；替换模式必须先下载原数据并确认。导入前检查格式、版本、字段与关联，七张表在同一事务提交或回滚。
- [x] 文件读取：中文 RTF 解码；空扫描 PDF 不假成功；手写 PDF 按六页一批调用视觉识别，DeepSeek 明确提示不支持；PDF/Word/导出组件按需加载。
- [x] 离线：保留当前缓存，只清理 KaoBuddy 旧缓存；不再每次打开注销 SW；升级提示后手动刷新，避免丢失编辑内容。
- [x] 请求安全：默认限制公网目的地址，DNS 校验并固定连接 IP，重定向逐跳校验；本地 AI 只通过显式环境变量放行。
- [x] 自动测试：前端单测加入 npm test 与 CI；补事务回滚、恶意备份、中文解析、扫描 PDF、缓存更新、请求安全回归。
- [x] 学习续接：按模块保存卡片轮次/反馈、按项目保存速背结果；解决最后一张反馈读取旧 state 的问题。
- [x] 手机与文案：压缩头部，项目选择折叠，全部导航可见；过期考试显示已结束；修正文档入口、请求流向和版本号。
- [x] 全量验证：pytest、py_compile、npm test、tsc、Vite build；浏览器测试备份导入、无效备份、桌面和390px窄屏、刷新续接；将真实外部服务未验证项如实写明。
- [x] 按有意义的进度块提交并推送，创建关联 #64 的 PR，保留原有未提交修改。

测试命令：`.venv/bin/pytest -q`、`python3 -m py_compile backend/app/*.py`、`npm test`、`npm run build`。
